<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.goott.bridge.member.MemberDaoInterface">
	<insert id="insertRecord" parameterType="kr.goott.bridge.member.MemberVO">
		insert into member(userNum, userMail, userPwd, userName, userRegdate, mailChk)
		values (memberSq.nextVal, #{userMail}, #{userPwd}, #{userName}, sysdate, #{mailChk})
	</insert>
</mapper>